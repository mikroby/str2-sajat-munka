import { DatesorterPipe } from './datesorter.pipe';

describe('DatesorterPipe', () => {
  it('create an instance', () => {
    const pipe = new DatesorterPipe();
    expect(pipe).toBeTruthy();
  });
});
